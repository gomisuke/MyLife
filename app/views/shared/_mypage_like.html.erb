<div class="row  overflow-auto mypage-box">
	<% @like_posts.each do |post| %>
	<div class="col-4">
		<%= attachment_image_tag(post, :post_image, :fill, 100, 100, format: "jpeg", fallback: "no-image-camera.png", class:"card-img-top") %>
	</div>
	<% end %>
</div>