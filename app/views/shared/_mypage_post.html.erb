<div class="row overflow-auto mypage-box">
	<% @posts.each do |post| %>
	<div class="col-4">
		<%= attachment_image_tag(post, :post_image, :fill, 200, 200, format: "jpeg", fallback: "no-image-camera.png", class:"card-img-top") %>
	</div>
	<% end %>
</div>